appId: com.natkibe.videoplayerpro.s26ultra
---
# Milestone 07 smoke: launch, grant media permission when prompted, verify main screen.
- launchApp
- runFlow:
    when:
      visible: "Allow"
    commands:
      - tapOn: "Allow"
- runFlow:
    when:
      visible: "While using the app"
    commands:
      - tapOn: "While using the app"
- assertVisible: "VideoPlayer Pro"
- assertVisible: "Main folder screen"
- assertVisible:
    id: "com.natkibe.videoplayerpro.s26ultra:id/recycler"
